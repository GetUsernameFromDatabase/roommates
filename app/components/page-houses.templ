package components

templ PageHouses(pwi SPageWrapper) {
	@HtmlWrap(templ.Join(
		HeaderComponent(""),
		PageWrapper(pwi, HousesPageContent()),
	))
}

templ HousesPageContent() {
	WIP -- Houses PAGE -- WIP
}
