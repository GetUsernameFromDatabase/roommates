package components

templ PageMessaging(pwi SPageWrapper) {
	@HtmlWrap(templ.Join(
		HeaderComponent(""),
		PageWrapper(pwi, MessagingPageContent()),
	))
}

templ MessagingPageContent() {
	WIP -- Messaging PAGE -- WIP
}
