package components

import "roommates/db/dbqueries"

templ PageMain(pwi SPageWrapper, houses []dbqueries.House) {
	@HtmlWrap() {
		@HeaderComponent("")
		@PageWrapper(pwi) {
			@MainPageContent(houses)
		}
	}
}

templ MainPageContent(houses []dbqueries.House) {
	@houseAside(houses)
	<div class="bg-secondary content">
		LUUKE THERE IS NOTHING
	</div>
}
