package components

templ PageMain(pwi SPageWrapper) {
	@HtmlWrap(templ.Join(
		HeaderComponent(""),
		PageWrapper(pwi, MainPageContent()),
	))
}

templ MainPageContent() {
	WIP -- MAIN PAGE -- WIP
}
