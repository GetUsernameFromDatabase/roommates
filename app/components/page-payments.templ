package components

templ PagePayments(pwi SPageWrapper) {
	@HtmlWrap(templ.Join(
		HeaderComponent(""),
		PageWrapper(pwi, PaymentsPageContent()),
	))
}

templ PaymentsPageContent() {
	WIP -- Payments PAGE -- WIP
}
